from .p2p import *

class p2p:
    def host(config, username, ChoiceStyle):
        host(config, username, ChoiceStyle)
    def connect(config, username, ChoiceStyle):
        connect(config, username, ChoiceStyle)
